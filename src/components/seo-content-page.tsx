import { Link } from "@tanstack/react-router";
import { ArrowRight, Check, MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { seoPages } from "@/lib/seo-pages";
import { whatsappLink } from "@/lib/site";
export function SeoContentPage({ path }: { path: keyof typeof seoPages }) { const [title,, intro, points] = seoPages[path]; return <><section className="border-b border-border bg-muted/40"><div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><p className="text-xs font-semibold uppercase text-teal">Irrigation equipment and advice</p><h1 className="mt-4 max-w-4xl font-serif text-4xl font-bold sm:text-5xl">{title}</h1><p className="mt-5 max-w-2xl leading-7 text-muted-foreground">{intro}</p><div className="mt-7 flex flex-wrap gap-3"><Button asChild size="lg"><Link to="/products">Browse products <ArrowRight className="size-4" /></Link></Button><Button asChild size="lg" variant="outline"><a href={whatsappLink(`Hello DripTech, please help me with ${title.toLowerCase()}.`)} target="_blank" rel="noreferrer"><MessageCircle className="size-4" />Request advice</a></Button></div></div></section><section className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><h2 className="font-serif text-2xl font-bold">What a complete solution considers</h2><div className="mt-6 grid gap-5 md:grid-cols-3">{points.map((point) => <article key={point} className="border-t-2 border-teal pt-5"><Check className="size-5 text-teal" /><h3 className="mt-4 font-serif text-lg font-bold">{point}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Our team checks this against your farm, crop and water source before recommending equipment.</p></article>)}</div><div className="mt-12 bg-brand-dark px-6 py-8 text-brand-light sm:px-8"><h2 className="font-serif text-2xl font-bold">Get an accurate recommendation</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-brand-light/75">Share your county, acreage, crop, water source and available power. We will help identify suitable equipment and current pricing.</p></div></section></>; }
